/// @description Transfer id to delete button

// If this slot is selected, transfer instance_id to delete button
if (obj_SelectCharacterController.selected = saveSlot){
	obj_DeleteCharacterButton.deleteID = instance_id;
}