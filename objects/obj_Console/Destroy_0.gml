/// @description Clean Up

// Allow user free input again
input.free = true;

// Send message to message box
obj_MessageBox.newMessage = inputText;

// Stores the last string you sent to the console (retrieve with tab)
obj_Global.consoleStringPrevious = inputText;